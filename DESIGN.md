# 前端设计系统与交互规范

> 状态：规范性文件
>
> 适用范围：`apps/web/` 中的生产界面，以及后续新增的前端页面和组件
>
> 设计方向：将 iOS / Apple 原生产品的设计语言转译为桌面知识工作台，而不是模拟放大的手机界面
>
> 当前主题：仅浅色模式

## 1. 目的与权威性

本文件是仓库前端视觉、交互和内容设计的唯一规范来源。任何生产 UI 的新增、修改、Figma 还原、截图还原或原型转生产，都必须遵循本文件。

- `DESIGN.md` 定义设计意图、tokens、组件行为、页面模式和验收标准。
- `AGENTS.md` 定义工程协作、架构和验证流程，并强制前端遵循本文件。
- 已存在但不符合本文件的界面不构成新实现的先例；修改到相关区域时应在任务边界内逐步收敛。
- 若业务需求与本文件冲突，先停止实现，明确冲突和建议方案，取得用户确认后同步更新本文件，再继续开发。
- 隐私、安全、数据完整性、无障碍和仓库架构边界优先于纯视觉要求。
- 诊断实验页、测试夹具和设计探索只有在任务明确声明例外时才能偏离视觉规范，但仍必须满足无障碍、内容真实性和安全要求。

## 2. 产品与设计原则

### 2.1 产品定位

本产品是本机优先的知识工作台，核心任务是浏览 Vault 状态、管理资料与任务、进行会话并核验证据。界面应安静、直接、可扫描，适合长时间和高频使用。

### 2.2 核心原则

1. **内容与操作优先**：界面服务于工作流，不做营销展示。
2. **原生感而非仿制**：借用 iOS 的层级、触感、控件和动效，不复制 iPhone 外壳或把桌面界面做成单列手机页面。
3. **扁平但有层级**：主要依靠间距、排版、分隔线和状态变化建立层级，阴影只用于覆盖层。
4. **少解释，多行动**：删除口号、英文眉题、功能介绍和重复说明；只保留完成任务所需的信息。
5. **状态连续**：刷新、切换和提交不应闪白、清空或造成布局跳动。
6. **一致胜过新奇**：同类操作必须使用相同组件、位置、命名和反馈方式。
7. **真实而克制**：不编造数据、评价、指标或装饰内容，不用视觉噪声填补空白。
8. **全端可操作**：桌面端保持工作台密度，移动端完整可操作，但使用逐层导航而非压缩桌面布局。

## 3. 视觉语言

### 3.1 基础气质

关键词：安静、精确、轻盈、可信、克制、快速。

禁止把以下风格当作默认方案：

- 大型 hero 标题、品牌口号或营销式首屏；
- 英文大写眉题、过度字距或技术感装饰文案；
- 紫色或多色渐变、光晕、渐变球、bokeh；
- 满屏玻璃拟态、深色霓虹或通用 SaaS 仪表盘风格；
- 卡片套卡片、每个区块都做悬浮卡片；
- Emoji 图标、手绘 SVG 图标或无意义装饰图标；
- 为了“丰富”而添加的统计、标签、状态点或说明段落。

### 3.2 色彩 tokens

所有生产样式必须通过语义化 CSS 变量使用颜色。禁止在组件规则中重复硬编码十六进制颜色。

| Token | 默认值 | 用途 |
| --- | --- | --- |
| `--color-canvas` | `#f2f2f7` | 应用背景 |
| `--color-surface` | `#ffffff` | 主内容面 |
| `--color-surface-muted` | `#f8f8fa` | 次级区域、分组背景 |
| `--color-fill` | `#e5e5ea` | 未选中控件、轻填充 |
| `--color-fill-hover` | `#dcdce2` | 中性悬停状态 |
| `--color-text` | `#1c1c1e` | 主文字 |
| `--color-text-secondary` | `#545458` | 次级文字 |
| `--color-text-tertiary` | `#6e6e73` | 辅助信息、占位符 |
| `--color-text-disabled` | `#8e8e93` | 禁用控件文字，不用于普通正文 |
| `--color-separator` | `rgb(60 60 67 / 18%)` | 普通分隔线 |
| `--color-separator-strong` | `rgb(60 60 67 / 32%)` | 强分隔线、输入边界 |
| `--color-accent` | `#0066cc` | 主操作、选中、可交互焦点 |
| `--color-accent-hover` | `#0055ad` | 主操作悬停 |
| `--color-accent-soft` | `#eaf3ff` | 选中背景、浅色次按钮 |
| `--color-success` | `#248a3d` | 成功、健康 |
| `--color-warning` | `#9a6700` | 警告、需处理 |
| `--color-danger` | `#d70015` | 失败、删除、不可逆风险 |
| `--color-focus-ring` | `rgb(0 102 204 / 32%)` | 键盘焦点环 |

规则：

- 中性色必须占据绝大多数可视面积，蓝色只标识交互和选中。
- 绿色、黄色和红色只表达状态，不承担品牌装饰作用。
- 小字号文本与背景对比度至少达到 WCAG AA 4.5:1，大字号至少 3:1。
- 不通过颜色单独表达状态；必须同时提供文字、图标或结构差异。
- 本次只定义浅色模式。不得用简单反色方式临时增加深色模式。

### 3.3 字体与排版

字体栈：

```css
font-family: "SF Pro Text", "SF Pro Display", "Segoe UI Variable", "Segoe UI", sans-serif;
```

不加载远程字体。Windows 默认使用 `Segoe UI Variable`，Apple 平台优先使用 SF 系列。

| 层级 | 字号 / 行高 | 字重 | 使用场景 |
| --- | --- | --- | --- |
| Display | `28px / 36px` | 650 | 页面主标题，仅一处 |
| Title 1 | `24px / 32px` | 650 | 重要页面或详情标题 |
| Title 2 | `20px / 28px` | 600 | 主要分区标题 |
| Title 3 | `17px / 24px` | 600 | 面板、列表分组标题 |
| Body | `15px / 24px` | 400 | 默认正文与控件文字 |
| Callout | `14px / 20px` | 500 | 状态、摘要、表格内容 |
| Caption | `12px / 18px` | 400 | 时间、补充信息 |

排版规则：

- 字距统一为 `0`，禁止负字距和装饰性拉宽字距。
- 中文正文使用自然换行，推荐 `text-wrap: pretty`；标题可使用 `text-wrap: balance`。
- 页面标题必须是产品对象或当前任务的直接名称，不使用口号。
- 单个页面主标题通常不超过一行；紧凑面板内禁止使用 Display 级字号。
- 技术 ID、哈希、协议字段只在诊断或明确需要时使用等宽字体，不将等宽字体当作装饰。

### 3.4 间距、尺寸与圆角

间距使用 4px 基准：

| Token | 值 |
| --- | --- |
| `--space-1` | `4px` |
| `--space-2` | `8px` |
| `--space-3` | `12px` |
| `--space-4` | `16px` |
| `--space-5` | `20px` |
| `--space-6` | `24px` |
| `--space-8` | `32px` |
| `--space-10` | `40px` |

圆角：

| Token | 值 | 使用场景 |
| --- | --- | --- |
| `--radius-small` | `6px` | 标签、小型控件 |
| `--radius-control` | `8px` | 按钮、输入框、分段控件 |
| `--radius-panel` | `8px` | 独立重复项或工具面板 |
| `--radius-sheet` | `16px` | 移动端 Sheet 顶角、模态覆盖层 |
| `--radius-pill` | `999px` | 状态胶囊、分段选项；不得用于普通按钮 |

尺寸规则：

- 桌面普通控件高度为 36–40px；移动端可点击目标不得小于 44×44px。
- 图标常用 16、18、20px；导航主图标可使用 20px。
- 固定格式控件必须定义稳定的宽高或网格轨道，状态变化不得推动布局。
- 普通内容区不得依靠超大留白营造高级感；留白必须服务于层级和可读性。

### 3.5 边框、阴影与材质

- 默认使用 1px 分隔线，不给每个内容块增加完整边框。
- 页面内联区块不使用悬浮阴影。
- 阴影仅用于 Drawer、Sheet、Popover、Menu 和 Dialog 等覆盖层。
- 推荐覆盖层阴影：`0 20px 50px rgb(28 28 30 / 18%)`。
- 半透明与 `backdrop-filter` 只用于固定工具栏、移动端 Sheet 和覆盖层；主要内容面保持纯色。
- 不允许多层模糊、彩色阴影或玻璃卡片堆叠。

## 4. 布局与响应式

### 4.1 全局应用外壳

- 桌面宽度 `>= 1100px`：保留固定左侧导航和顶部上下文栏。
- 左侧导航建议宽度 188–208px；使用图标加短标签，当前项用浅蓝填充和蓝色图标表示。
- 顶部上下文栏高度建议 48–52px，只展示当前位置、当前 Vault 和必要的全局状态。
- 普通工作区使用受约束的最大宽度；会话等多栏工作流可使用完整可用宽度。
- 页面不得以大型 hero 区域开场。标题、主要操作和状态应在首屏紧凑呈现。

### 4.2 桌面、平板与移动端

| 断点 | 布局规则 |
| --- | --- |
| `>= 1100px` | 固定导航；多栏工作台；完整表格或列表 |
| `821–1099px` | 导航进入侧边 Drawer；按任务保留一至两栏 |
| `<= 820px` | 单层页面；逐层进入详情；证据、筛选和次级操作使用 Sheet |
| `<= 480px` | 以 390px 宽度为主要验收基准；所有操作可达且无横向溢出 |

移动端规则：

- 不把桌面三栏纵向堆成长页面。
- 会话页以会话列表、正文、应用证据三个独立层级切换；证据默认使用底部 Sheet。
- 主要操作可固定在安全区域上方，但不得遮挡内容或输入法。
- 表格在移动端转为有明确标签的行列表，不依赖横向滚动完成核心任务。

### 4.3 会话三栏

桌面会话区保持以下结构：

1. 历史栏：稳定宽度 232–272px。
2. 正文栏：`minmax(420px, 1fr)`，承担阅读与输入。
3. 应用证据栏：稳定宽度 280–340px。

栏宽、标题和空状态变化不得造成整体跳动。低频操作放入更多菜单，不挤占标题区。

## 5. 组件标准

### 5.1 图标

- 统一使用 `lucide-react`，版本必须锁定并写入 `apps/web/package.json`。
- 禁止手绘 SVG、Emoji、图标字体或混用多套图标库。
- 图标默认继承文字颜色；状态图标使用语义色 token。
- 图标按钮必须提供 `aria-label`；不熟悉的图标必须提供 Tooltip。
- 装饰性图标使用 `aria-hidden="true"`，不得进入可访问名称。

### 5.2 按钮

按钮只用于明确命令，标签使用动词或动宾短语。

| Variant | 视觉 | 使用场景 |
| --- | --- | --- |
| Primary | 蓝色实底、白字 | 当前页面唯一或最主要操作 |
| Secondary | 浅蓝底、蓝字 | 重要但非主操作 |
| Ghost | 透明底、中性色；悬停轻填充 | 工具栏和低频操作 |
| Icon | 正方形 Ghost，仅图标 | 返回、关闭、刷新、搜索、更多 |
| Danger | 红字或红色实底 | 删除、移除、不可逆操作 |

规则：

- 同一可视区域通常只有一个 Primary 按钮。
- 返回、关闭、刷新、搜索和更多使用熟悉的 Lucide 图标，不使用文字矩形按钮。
- 创建、导入、发送、保存、删除等关键命令保留文字。
- 普通按钮圆角为 8px，不使用胶囊形；分段控件和状态标签可以使用胶囊形。
- 按下反馈不得改变控件占位尺寸。
- 禁用状态必须保留可读标签，并通过 `disabled` 或 `aria-disabled` 表达。

### 5.3 选择控件

- 2–5 个互斥模式使用 Segmented Control。
- 二元设置使用 Switch；批量选择使用 Checkbox。
- 选项较多或低频选项使用 Select 或 Menu。
- 数值范围使用 Slider、Stepper 或数字输入，不使用多个文字按钮代替。
- 当前选择必须同时具备背景、文字或图标差异，不能只依赖颜色。

### 5.4 输入框与表单

- 标签常驻，不以 placeholder 代替字段名。
- 输入框默认高度 40px，移动端至少 44px；文本域根据内容增长但必须有最大高度。
- 帮助内容只解释限制、格式或风险，不复述字段名。
- 验证错误放在对应字段附近，并说明如何修复。
- 保存中保持输入内容可见；成功不清空用户上下文。
- 设置页按主题分组，危险操作独立放在末尾，不与常用设置混排。

### 5.5 列表、表格与卡片

- 可扫描数据优先使用列表或表格，不默认转换为卡片网格。
- 列表行用分隔线和对齐建立结构；整行可点击时必须有悬停、焦点和按下状态。
- 卡片只用于独立重复项、模态内容或真正需要边界的工具，不把页面分区全部卡片化。
- 禁止卡片内再嵌套视觉卡片。
- 状态、标题、数量和时间必须有稳定列宽，加载或状态变化不得导致列跳动。

### 5.6 状态标签与进度

- 状态使用短文本，例如“健康”“运行中”“需处理”“失败”。
- 状态色只作为辅助，文字必须始终存在。
- 进度条用于可估算流程；未知时长使用克制的活动指示，不伪造百分比。
- 同一对象不得在相邻位置重复展示同一状态。
- 内部 ID、哈希和 graph/chunk locator 不在生产 UI 中直接展示。

### 5.7 Drawer、Sheet、Dialog 与 Menu

- Drawer 用于桌面二级上下文，保持用户对底层页面的空间认知。
- Sheet 用于移动端证据、筛选、更多操作和短表单。
- Dialog 只用于必须打断流程的确认或小型任务，不承载长页面。
- Menu 用于低频命令，不隐藏主要操作。
- 覆盖层打开后焦点进入，关闭后回到触发控件；Escape 可关闭非强制覆盖层。
- 危险操作确认必须说明对象和后果，不使用泛化的“确定吗”。

### 5.8 空、错与加载状态

每个异步界面必须覆盖：

- `loading`：首次加载，无现有内容；
- `empty`：请求成功但没有对象；
- `success`：内容可用；
- `recoverable`：内容或操作可恢复，提供下一步；
- `failed`：无法继续，说明原因和重试方式。

规则：

- 首次加载可使用稳定尺寸的 Skeleton。
- 后台刷新时保留旧内容，只在工具栏或相关区域显示细小刷新状态。
- 刷新失败时保留旧内容，标注其更新时间并提供重试。
- 不使用整页闪白、整页 Spinner 或清空后重建的方式刷新。
- 空状态只说明发生了什么和可执行动作，不写产品功能介绍。

## 6. 页面模式

### 6.1 工作台

- 页面标题直接使用“工作台”，不使用英文眉题或宣传口号。
- 顶部展示紧凑摘要和刷新动作，摘要只包含真实、可行动的数据。
- Vault 使用可扫描列表或表格；点击进入二级 Drawer。
- “需处理”和“最近活动”作为轻量列表，不使用装饰卡片。
- 说明 Vault 隔离边界的文字仅在存在误操作风险的位置出现一次。

### 6.2 资料

- 以资料库列表和详情为主，不用封面图或装饰缩略图填充。
- 创建、切换、重新授权和导入操作位置稳定。
- 策略、索引、标签和图谱按任务分组，低频内容进入详情或次级导航。

### 6.3 会话

- 桌面保留“历史 / 正文 / 应用证据”三栏。
- 正文是视觉主区域；检索与证据术语不抢占回答层级。
- 输入区固定、稳定且不遮挡最后一条内容。
- 移动端正文与证据分层进入，证据使用底部 Sheet。
- 刷新会话详情不得清空已显示内容或夺取阅读位置。

### 6.4 任务

- 首屏突出任务队列、当前状态、进度和需要用户处理的事项。
- 已完成、排队、运行中和失败状态具有一致位置和词汇。
- 任务详情聚焦源解析、结构化结果和恢复动作，不展示内部实现说明。
- 删除与重试不得造成列表整体抖动或无关条目重排。

### 6.5 设置

- 采用原生设置列表的层级：分组标题、字段、当前值和必要说明。
- Provider、模型和预算设置按任务域分组，不把所有字段铺成同权重表单。
- 连接测试必须由用户明确触发；状态和错误紧邻对应 Provider。
- 危险操作独立分组并放在页面末尾。

## 7. 内容与文案

### 7.1 文案原则

- 默认使用中文；模型名、协议名、代码标识和必要专有名词保留英文。
- 标签短、具体、可扫描；按钮使用明确动词。
- 删除眉题、口号、欢迎语、功能介绍和重复解释。
- 错误文案使用“发生了什么 + 为什么 + 下一步”的结构。
- 风险提示只在操作可能扩大权限、发送内容、删除数据或改变不可恢复状态时出现。
- 不用“智能”“强大”“一站式”“轻松”等营销词描述功能。

### 7.2 禁止内容

- 编造的指标、用户评价、成功率或引用；
- 为填空而添加的说明段落；
- 标题前的 Emoji 或装饰性图标；
- 将内部实现、数据库身份或哈希直接暴露给普通用户；
- 在主界面解释键盘快捷键、设计理念或“如何使用本页面”。

## 8. 动效与状态连续性

### 8.1 目标

动效质量是一级验收标准。目标是操作连续、响应即时、稳定 60fps，而不是增加动画数量。

### 8.2 Motion tokens

| Token | 值 | 使用场景 |
| --- | --- | --- |
| `--motion-instant` | `90ms` | 按下、焦点、颜色反馈 |
| `--motion-fast` | `160ms` | Hover、选中、轻量控件 |
| `--motion-standard` | `220ms` | 面板内容切换 |
| `--motion-panel` | `280ms` | Drawer、Sheet、页面层级切换 |
| `--ease-standard` | `cubic-bezier(0.2, 0.8, 0.2, 1)` | 默认进入和移动 |
| `--ease-exit` | `cubic-bezier(0.4, 0, 1, 1)` | 退出 |

规则：

- 主要动画只改变 `transform` 和 `opacity`。
- 禁止用 `top`、`left`、`width`、`height` 或大面积阴影变化制作持续动画。
- 按钮按下可使用 `scale(0.98)`，释放后立即恢复；不得弹跳。
- Drawer 和 Sheet 使用位移加淡入，底层内容只做轻微遮罩，不缩放成装饰舞台。
- 页面层级切换优先使用共享元素或原位置连续过渡。
- 可渐进增强使用 View Transitions API；不支持时必须有无跳动的 CSS fallback。
- 不滥用 `will-change`，仅在动画即将开始的元素上短暂使用。
- 所有动效必须遵守 `prefers-reduced-motion: reduce`，减少动态时取消位移和缩放，仅保留必要的即时状态变化。

### 8.3 异步交互

- 点击后在 100ms 内提供视觉反馈。
- 能本地确定的选择、展开和切换立即响应；网络结果随后同步。
- 请求期间保持当前内容和滚动位置，禁止无理由重置选择。
- 成功更新采用局部替换，不重新挂载整个页面。
- 失败保留可恢复上下文，不伪装成成功或静默回滚。

## 9. 无障碍

- 所有交互元素必须通过键盘访问，使用正确的原生语义元素。
- `:focus-visible` 焦点环清晰且不被裁切。
- 图标按钮必须有可访问名称；表单控件必须有持久标签。
- 点击目标桌面不小于 36×36px，移动端不小于 44×44px。
- 文本缩放至 200% 时不得遮挡、溢出或丢失操作。
- 不用 Hover 承载唯一信息；Tooltip 只是补充。
- Drawer、Sheet、Dialog 必须管理焦点、Escape 和背景交互。
- 状态更新使用适当的 `aria-live`，避免对连续刷新进行噪声播报。
- 动效遵守减少动态偏好，颜色遵守 WCAG AA。

## 10. 工程实现规则

- 保持 React/Vite 单页架构，不为视觉改造引入新的状态库或 UI 框架。
- 共享视觉规则通过 CSS variables 和小型 React primitives 复用；不要在页面组件中复制按钮、图标、Sheet 或状态样式。
- Design tokens 当前集中定义在 `apps/web/src/style.css` 顶部；若后续拆分，使用单一 token 文件并由入口样式导入。
- 新增或修改的生产 UI 不得硬编码颜色、圆角、阴影、间距层级和动效时长；使用本文件定义的 tokens。
- 组件命名使用 PascalCase，CSS class 使用 kebab-case；variant 名称使用语义而非颜色名称。
- UI 不复制后端业务判定，不根据展示字段重新推断任务状态或权限。
- 不添加远程字体、远程图标或无业务必要的图片请求。
- Figma 或截图仅作为视觉输入；实现必须转换为本仓库的 React、CSS、tokens 和组件模式，不直接粘贴外部框架代码。
- 若 Figma 与 `DESIGN.md` 冲突，以 `DESIGN.md` 为准；需要改变规范时先取得确认并更新本文件。

## 11. 验证与验收

### 11.1 自动验证

前端 UI 改动至少运行：

```powershell
npm --prefix apps/web run test
npm --prefix apps/web run lint
npm --prefix apps/web run build
npm run browser-test
npm run progress:build
npm run progress:check
git diff --check
```

### 11.2 视觉与交互验证

Playwright 至少覆盖以下视口：

- Desktop：1440×900；
- Compact desktop / tablet：1024×768；
- Mobile：390×844。

交付前必须人工或截图检查：

- 无重叠、横向溢出、文字裁切和布局跳动；
- 页面标题、主要操作和当前状态首屏可见；
- 键盘焦点顺序正确，图标按钮有名称；
- Drawer、Sheet、Dialog 可打开、关闭并恢复焦点；
- 刷新保留旧内容和滚动位置；
- loading、empty、success、recoverable、failed 状态完整；
- 390px 下所有核心功能可达；
- `prefers-reduced-motion` 下无非必要位移或缩放；
- 动画过程没有明显掉帧、闪白或重排抖动；
- 控制台无错误，关键点击流程通过。

### 11.3 UI 完成定义

只有同时满足以下条件，UI 任务才能视为完成：

1. 符合本文件的 tokens、组件、内容和页面模式；
2. 桌面与移动端核心流程完整可操作；
3. 异步状态连续，错误可恢复；
4. 无障碍和减少动态要求通过；
5. 前端单测、构建和关键 Playwright 流程通过；
6. 视觉截图已检查，未发现重叠、溢出或明显 AI 模板感；
7. 任何例外已在任务 `executionBoundary` 中记录并获得用户确认。

## 12. 变更治理

- 修改设计 tokens、全局布局、组件变体、响应式策略或动效规则时，必须同步更新本文件。
- 单页的特殊需求不能静默成为全局规范；需要复用时再提升为 token 或 shared primitive。
- 设计例外必须说明原因、影响范围、退出条件和后续归属。
- 评审 UI 时优先检查：功能性、层级、连续性、可访问性、一致性，再检查装饰细节。
- 规范应保持可执行；发现规则过时、冲突或无法验证时，应在同一任务中修订，而不是绕过。
